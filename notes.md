第一讲 初识C语言
1.安装编程软件
注意将软件，文件等缓存到D盘；安装路径不要有中文
2.创建一个工程
创建file文件夹并创建快捷方式至桌面，创建C语言工程总文件夹c-projrct,创建C语言第一个工程文件夹project1
3.编写一个简单的代码
打开小熊猫，点击文件打开新建，新建一个项目；然后选择控制台程序，选择C语言项目，项目名称命名为“project1”，创建在D盘的file中的c-project的project1中，点击OK：再将光标移动到return0前面，按回车键空几格出来，将代码写在中间空白处即可：在空白处打出"printf"的字样，用英文打出括号和引号，在引号中打出hello micu后在括号后打出分号，一个简单的代码就完成了
4.转义序列
如果想打出hello micu,hello may则需要用到转义序列，即打出printf("hello micu  hello may”)以后需要在中间打出\n即可；\n是换行，\加字母组成了转义序列
5.变量与赋值
例如声明一个整数a，就是int a，声明一个小数b,就是float b;注意变量的声明必须在使用前，以分号结尾，如将其赋值为int a=1；float b=3.14；变量的声明应在函数外面，一切的操作写在main函数里面
6.变量的运算
注意乘号以*表示，除号以/表示,取余用%表示
7.用C代码解决实际问题
注意变量的取名规范
8.显示变量的值
%是占位符，作用是帮忙显示变量；int类型的占位符示是%d，float类型的占位符是%f；例如printf("体积：%d",volum);
9.读入用户输入内容赋值给变量
print是格式化输出的函数，scanf是格式化输入的函数，scntf一定要加取地址的符号&，例如scantf("%d",&lenth);
10.完善程序
给予用户中文提示，如printf("请输入物体的长:");
11.注释
//是单行有效，/*是多行有效，作用是增加代码的易读性
12.整体代码结构分析
<1>预处理指令之一，调用标准功能库
#inciude 包含；#define 宏定义，可以将前面等价于后面
<2>声明变量
命名规则：只能包含英文字母，数字，下划线；不能以数字开头，必须以英文字母或者下划线开头；不能和关键字重复；要区分大小写；可以一次性声明多个，中间用逗号隔开
<3>main函数，有且仅有一个
<4>格式化输入/输出函数
%n.md %n.mf  n表示最小栏宽，如果不足将会左侧补空格，超过则正常显示；前者m代表显示有效位数，不足补0，超过则正常显示，后者表示小数点后有效位数
<5>运算语句

第二讲表达式和选择语句
1.表达式
<1>c语言中每一行，有分号就是一个语句，由每一行的表达式去组成语句
<2>int型和float型的结果是float，%操作不能是浮点数，只能是整数；/和%用作负数时，结果难以确定(此为C89之前的标准)
<3>运算符的优先级和结合性
想先算什么直接加括号
<4>赋值运算符
把=右边的值赋给左边，运用第一讲的知识
<5>复合赋值
例如a=++b；即先++再赋值，若为a=b++;即为先赋值再++
<6>自增运算符和自减运算符
2.选择语句
<1>关系运算符<,>,=
结果只有真假，真为1，假为0
<2>判等运算符（==为等于，！=为不等于）
<3>逻辑运算符（！为逻辑非，&&为逻辑与，||为逻辑或）
<4>if语句
if（表达式）
   语句
   else
   语句
<5>级联式if语句
例如成绩判定系统，需要将if与else嵌套
<6>条件表达式
if(a)b;else c;
例如表达式1?表达式2：表达式3
如果表达式1为真，就选择表达式2，否则就选择表达式3
<7>布尔值（用于表达逻辑关系，只能表达0和1）
即只有0的时候是假的是0，其他任何数字都是1s
